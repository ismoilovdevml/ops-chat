# OpsChat Environment Variables

# Required: Generate with `mix phx.gen.secret`
SECRET_KEY_BASE=your_secret_key_here

# Server configuration
PHX_HOST=localhost
PORT=4000

# Database path (inside container)
DATABASE_PATH=/app/data/ops_chat.db
